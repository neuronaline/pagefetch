# PageFetch core runtime dependencies
# Install: pip install -r requirements.txt
# Browser/PDF support: pip install ".[browser]" / ".[pdf]"
# Canonical source: pyproject.toml — this file mirrors the core set.

aiosqlite>=0.20,<1
beautifulsoup4>=4.12,<5
httpx[http2,socks]>=0.27,<1
lxml>=5.2,<6
platformdirs>=4.2,<5
pyyaml>=6.0,<7
tldextract>=5.1,<6
